export interface IStory {
  title: string;
  owner_id: string;
  contend: string;
  wall: string;
  object_id: string;
  emoji: number;
  emoji_preset: number;
  create_time: string;
  is_accept: boolean;
}
